# Application DTOs (Data Transfer Objects)

이 폴더는 계층 간 데이터 전송을 위한 객체 정의를 포함합니다.

## 목적
- 도메인 모델을 외부(Presentation, Infrastructure)로 직접 노출하지 않기 위해 사용합니다.
- API 요청/응답 타입을 정의하거나, UI에 표시할 데이터 형식을 정의합니다.

## 예시
- `CreateUserRequestDto`
- `UserResponseDto`
